spring:
  application:
    name: chat-app-service-test

# Test Anthropic API configuration
anthropic:
  api:
    key: test-api-key
    base-url: https://api.anthropic.com
    model: claude-3-sonnet-20240229
    max-tokens: 100
    temperature: 0.5

# Logging for tests
logging:
  level:
    com.mychatbot: DEBUG
    org.springframework.web: DEBUG
